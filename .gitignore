node_modules/
*.log
.DS_Store
coverage/
dist/
*.tgz
.env
.env.*
!.env.example
.fuse_hidden*

PROMPT_claude-code-publish.md
# forma dogfood: the scaffold is regenerated by `forma doc`; ARCHITECTURE.md is the curated doc.
docs/architecture/ARCHITECTURE.scaffold.md

# stray working-tree artifacts, never part of the package
forma-holo-essence.patch
forma-social-preview.png

# Worktree degli agenti: repo git annidati, mai da tracciare (#43)
.claude/worktrees/
.playwright-mcp/

# The Control Room is generated (verify -> gen -> room). CI composes it; committing it would
# make every fixture that copies this repo depend on the briefing being freshly composed.
# Both paths: `forma room` with no --out writes to the working directory, and a 263KB copy of the
# briefing was committed to the root that way — stale in the commit that created it, carrying a
# wall clock in a tracked file, and invisible to CI, which composes its own.
docs/architecture/control-room.html
/control-room.html
