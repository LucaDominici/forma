// beta participates in the room through its issues and git history alone: the manifest gives it
// no model and no topology, so the briefing has no map for it and must say so rather than imply one.
export function main() {
  return 'beta'
}
